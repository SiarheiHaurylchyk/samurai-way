import React from 'react';

function Nav() {
    return (
        <nav className="nav">
            <a href="#">Profile</a>
            <a href="#">Message</a>
            <a href="#">New</a>
            <a href="#">Music</a>
            <a href="#">Settings</a>
        </nav>
    );
}

export default Nav;